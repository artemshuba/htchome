<UserControl x:Class="Clock.WidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Clock.Controls"
             xmlns:viewModel="clr-namespace:Clock.ViewModel"
             mc:Ignorable="d"
             d:DesignWidth="500" d:DesignHeight="200">
    <UserControl.DataContext>
        <viewModel:WidgetViewModel/>
    </UserControl.DataContext>
    <Grid>
        <ContentControl Content="{Binding}" ContentTemplate="{DynamicResource WidgetVisual}"/>
    </Grid>
</UserControl>
